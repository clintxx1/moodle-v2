import { BrowserRouter } from "react-router-dom";
import Router from "./router";

function App() {
  /**
   * TODO - fix routing
   * Additional work
   * when auth is applied
   */

  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}

export default App;
